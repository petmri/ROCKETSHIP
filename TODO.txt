DCE Python Port TODO (active)

Recently completed
- Added tiny fast DCE fixture and settings-matrix tests.
- Added MATLAB dce_preferences.txt bridge into Python defaults.
- Added CLI overrides: --dce-preferences and repeatable --set KEY=VALUE.
- Added Tofts ve map parity checks (downsample + optional full-volume).
- Added GUI path/file browse dialogs and one-click tiny-fixture default run config.
- Added Stage-D acceleration fallback chain: gpufit -> cpufit -> pure CPU.
- Added installer hardening:
  - prevents dependency backtracking during pyCpufit/pyGpufit install
  - clearer error on missing cpufit_constrained export
  - CUDA-aware release-asset auto-selection by local environment
- Added Phase-1 Python reliability tests:
  - installer asset-selection/CUDA detection logic tests
  - DCE CLI output/event contract end-to-end test on tiny fixture
- Added cross-platform Python portability CI job (Windows + macOS) while keeping parity checks on PR CI.
- Multi-model parity usability + masking fixes:
  - multi-model parity test now reports all model/check failures in one run (no fail-fast on first model)
  - low-Ktrans masking is now VE-only in multi-model parity (`ROCKETSHIP_PARITY_VE_KTRANS_MIN`, default `1e-6`)
  - removed unintended Ktrans-based masking from non-VE params (`vp`, `fp`)
  - investigation note: Tofts-only downsample parity currently passes strongly; largest remaining gaps cluster in `ex_tofts` Ktrans, `tissue_uptake`, and `2cxm` parity checks
- Patlak/tissue parity hardening:
  - fixed Python `model_patlak_linear` divide-by-zero crashes when `Cp` contains zeros
  - Stage-D tissue fit now seeds per-voxel `initial_value_ktrans` from Patlak estimate (MATLAB CPU path alignment)
- Multi-model parity gate split:
  - required-by-default models: `tofts`, `ex_tofts`, `patlak`
  - unstable-model checks remain diagnostic by default (`tissue_uptake`, `2cxm`, plus optional CPU-vs-MATLAB checks)
  - added env controls to promote all models/checks back to strict gating when needed (`ROCKETSHIP_PARITY_REQUIRE_ALL_MODELS=1`)
- Added GPUfit support for all models, performance better than matlab
- Added CPUfit support for all models, performance similar to GPUfit
- Installer for GPU/CPUfit for both python and matlab
- Migrated all tests to pyTest
- Imported OSIPI data and built tests around that
- Reorganized test assets:
  - moved `test_data` into `tests/data`
  - moved data-generation scripts into `tests/data/scripts`
  - moved `tests/osipi` into `tests/data/osipi`
- Improved pure-Python OSIPI reliability + performance:
  - unified `2cxm` to a single OSIPI-style fit path (quadratic resampling + bounded curve fit)
  - optimized pure-Python forward-model integration for `tofts`, `ex_tofts`, `patlak`, `tissue_uptake`, and `2cxm`
  - corrected OSIPI `fp` unit conversion handling to `ml/100ml/min` in reliability checks, fast-backend checks, and summary generation
- Completed script-level option parity audit:
  - mapped every key in `script_preferences.txt` to Python config/stage overrides
  - classified each key as `supported`, `intentionally_dropped`, or `pending`
  - added regression tests for newly wired script-level alias families
- Expanded dataset-backed DCE regression beyond Tofts maps:
  - added CPU parity checks for additional model maps (`ex_tofts`, `patlak`, `tissue_uptake`)
  - added ROI `.xls` parity checks against MATLAB ROI tables (`tofts`, `ex_tofts`, `patlak`, `tissue_uptake`)
- Hardened OSIPI fast-backend test gating:
  - `test_osipi_pygpufit.py` now skips unless CUDA GPUfit is actually available
  - avoids running gpufit fallback-path checks as if they were CUDA GPUfit reliability checks
- Improved parity/benchmark CLI output + naming consistency:
  - restored `run_dce_parity` post-run summary tables (corr/MSE/MAE + related parity metrics)
  - added `run_dce_benchmark.py` naming-aligned benchmark runner
- Moved contract parity tooling from `tests/python` to `tests/contracts`:
  - moved `generate_python_results.py` and `compare_with_matlab_baseline.py`
  - updated CI and docs to use new script paths
  - normalized script defaults to repo-root-based contract paths

Highest priority next
- Fix 2cxm and uptake in cpufit and gpufit
- Promote unstable-model parity checks to strict gating once stable:
  - probably won't fix matlab, so remove parity checks between these models and matlab
  - promote `tissue_uptake` from diagnostic to required
  - promote `2cxm` from diagnostic to required
- Add tiny fixture edge-case variants:
  - low-SNR
  - non-uniform timer
  - harsh bounds / low-iteration fit stress case

Secondary
- Decide on nested and FXL_rr support vs explicit non-support.
- DSC parity expansion for DSC_convolution_oSVD.
- GUI distribution plan for non-programmers:
  - evaluate packaged app targets (macOS app bundle, Windows installer)
  - keep venv/dev launch path as baseline during early iterations
