# Documentation build only. The site is built by .github/workflows/docs.yml and
# renders with Zensical, not MkDocs — see that workflow for the reasoning.
#
# Pinned: Zensical is pre-1.0 and its config surface is still moving.
zensical==0.0.57
